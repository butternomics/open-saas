---
phase: 01-foundation-and-connectors
plan: 07
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04", "01-05"]
files_modified:
  - butter-comment-assistant/src/butter_comment_assistant/main.py
  - butter-comment-assistant/src/butter_comment_assistant/web/__init__.py
  - butter-comment-assistant/src/butter_comment_assistant/web/routes_status.py
  - butter-comment-assistant/src/butter_comment_assistant/templates/status.html
  - butter-comment-assistant/README.md
  - butter-comment-assistant/tests/conftest.py
  - butter-comment-assistant/tests/test_status_screen.py
  - butter-comment-assistant/tests/test_smoke_foundation.py
autonomous: true
requirements: [FOUND-01, CONN-01, CONN-02, CONN-05]

must_haves:
  truths:
    - "User can start the app with one documented command, open it, and land on a connection status screen showing connector type with the MOCK badge, auth state, documented scopes, token presence, secret store backend, LLM health and last successful sync (FOUND-01, CONN-05, D-03, D-29)"
    - "The whole app runs end to end on MockConnector and FakeLLMClient built by the factories from BCA_CONNECTOR and BCA_LLM, and swapping to instagram or ollama needs only configuration and a stored token (CONN-01, D-23, D-28)"
    - "The MOCK badge appears on every registered GET page while MockConnector is active, and an auth failure renders as a distinct AUTH PROBLEM block, never as a quiet day (CONN-02, D-21, D-29)"
    - "README documents exactly one install command, one run command, the test command and every BCA_* variable (FOUND-01, D-03, D-12)"
  artifacts:
    - path: "butter-comment-assistant/src/butter_comment_assistant/web/routes_status.py"
      provides: "GET /status and POST /status/check"
      contains: "/status/check"
    - path: "butter-comment-assistant/src/butter_comment_assistant/templates/status.html"
      provides: "Connection status screen"
      contains: "data-testid=\"auth-state\""
    - path: "butter-comment-assistant/README.md"
      provides: "Install, run, test, configuration"
      contains: "uv venv --python 3.12 .venv"
    - path: "butter-comment-assistant/tests/test_smoke_foundation.py"
      provides: "End-to-end smoke of the walking skeleton"
      contains: "mock-badge"
  key_links:
    - from: "butter-comment-assistant/src/butter_comment_assistant/main.py"
      to: "connectors.factory.build_connector and llm.factory.build_llm_client"
      via: "lifespan builds app.state.connector, app.state.llm, app.state.secret_store from config and AppSettings"
      pattern: "build_connector\\(|build_llm_client\\("
    - from: "butter-comment-assistant/src/butter_comment_assistant/web/routes_status.py"
      to: "app.state.connector.check_auth"
      via: "cached AuthStatus refreshed by POST /status/check"
      pattern: "check_auth\\(\\)"
    - from: "butter-comment-assistant/src/butter_comment_assistant/web/__init__.py"
      to: "app.state.connector.is_mock"
      via: "shell_context reads the live connector instance"
      pattern: "is_mock"
---

## Phase Goal

**As a** Butter operator (Brandon), **I want to** start the comment assistant locally and see a clearly labeled MOCK connection with settings that persist, **so that** every later phase has a tested seam for storage, secrets, Instagram and the local model.

<objective>
Close the walking skeleton: wire the secret store, connector and LLM client factories into the app lifespan, add the connection status screen, write the README with the documented commands, and prove the whole foundation end to end with a smoke test.

Purpose: CONN-05 and CONN-01 are only true once the app runs on the real seams; FOUND-01 is only true once the commands are documented and the full suite passes.
Output: wired main.py, status route and template, README, conftest fixtures, status and smoke tests.
</objective>

<execution_context>
@/home/user/open-saas/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/open-saas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-connectors/01-CONTEXT.md
@.planning/phases/01-foundation-and-connectors/01-RESEARCH.md
@.planning/phases/01-foundation-and-connectors/01-SKELETON.md
@butter-comment-assistant/src/butter_comment_assistant/main.py
@butter-comment-assistant/src/butter_comment_assistant/config.py
@butter-comment-assistant/src/butter_comment_assistant/web/__init__.py
@butter-comment-assistant/src/butter_comment_assistant/secrets.py
@butter-comment-assistant/src/butter_comment_assistant/logging_setup.py
@butter-comment-assistant/src/butter_comment_assistant/connectors/factory.py
@butter-comment-assistant/src/butter_comment_assistant/connectors/base.py
@butter-comment-assistant/src/butter_comment_assistant/llm/factory.py
@butter-comment-assistant/src/butter_comment_assistant/storage/settings_repo.py

**Working directory:** `/home/user/open-saas/butter-comment-assistant`. If `.venv/bin/pytest` is missing in this worktree run: `uv venv --python 3.12 .venv && uv pip install --python .venv/bin/python -e ".[dev]"`.

Plan 01-06 (InstagramGraphConnector) runs in parallel with this plan; do not import `connectors.instagram_graph` directly. The factory's lazy import handles it, and this plan's tests only use `BCA_CONNECTOR=mock`.

<interfaces>
<!-- Contracts consumed (already on disk; read them) and created -->

Consumed:
- `config.load_config(env) -> ProcessConfig` (fields: data_dir, db_path, host, port, allow_non_loopback, connector, llm, secret_store, log_level)
- `secrets.resolve_secret_store(mode, data_dir) -> SecretStore` (`.get(name)`, `.backend_name`); `SECRET_INSTAGRAM_ACCESS_TOKEN`
- `logging_setup.configure_logging(level)`
- `connectors.factory.build_connector(kind, ConnectorOptions(api_version, dataset_path, token_provider, allow_non_loopback))`
- `connectors.base.AuthStatus`, `Connector.check_auth()`, `.is_mock`, `.display_name`, `.kind`
- `llm.factory.build_llm_client(kind, LLMOptions(host, text_model, vision_model, allow_non_loopback))`, `LLMClient.health() -> LLMHealth`
- `storage.settings_repo.SettingsRepo.load/get_state`, `STATE_LAST_SUCCESSFUL_SYNC_AT`, `AppSettings`
- `web.render(request, name, status_code=200, **ctx)`, `web.shell_context(request)`

Created:
```python
# main.py lifespan additions (after run_migrations):
#   configure_logging(config.log_level)
#   app.state.secret_store = resolve_secret_store(config.secret_store, config.data_dir)
#   with session_scope(engine) as s: settings = SettingsRepo.load(s)
#   app.state.connector = build_connector(config.connector, ConnectorOptions(api_version=settings.instagram_api_version, token_provider=lambda: app.state.secret_store.get(SECRET_INSTAGRAM_ACCESS_TOKEN), allow_non_loopback=config.allow_non_loopback))
#   app.state.llm = build_llm_client(config.llm, LLMOptions(host=settings.ollama_host, text_model=settings.ollama_text_model, vision_model=settings.ollama_vision_model, allow_non_loopback=config.allow_non_loopback))
#   app.state.auth_status: AuthStatus | None = None   (cache filled lazily by /status)
#   include web.routes_status.router

# web/routes_status.py
router = APIRouter()
async def get_status_context(request) -> dict   # connector_kind, connector_label, is_mock, auth (AuthStatus, computed once and cached on app.state.auth_status), scopes (auth.scopes_documented or DOCUMENTED_SCOPES), token_present (bool via secret_store.get(SECRET_INSTAGRAM_ACCESS_TOKEN) is not None), secret_backend, last_sync (SettingsRepo.get_state(STATE_LAST_SUCCESSFUL_SYNC_AT) or None), llm_kind, llm_health (LLMHealth), global_pause, automation_enabled
@router.get("/status")            # renders status.html
@router.post("/status/check")     # CSRF protected by middleware; sets app.state.auth_status = await connector.check_auth(); re-renders status.html (status 200)
```

templates/status.html test ids: `connector-kind`, `auth-state` (text "CONNECTED" when ok, "AUTH PROBLEM" when not), `auth-error` (only when not ok, inside `<div class="alert-auth">`), `scopes` (one `<li>` per scope), `token-present` ("yes"/"no"), `secret-backend`, `last-sync` ("never" when null), `llm-kind`, `llm-health` ("ok"/"unavailable"), and a form `POST /status/check` with the hidden csrf field and `hx-post`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire secret store, connector and LLM factories into the app lifespan and shell context</name>
  <files>butter-comment-assistant/src/butter_comment_assistant/main.py, butter-comment-assistant/src/butter_comment_assistant/web/__init__.py, butter-comment-assistant/tests/conftest.py</files>
  <read_first>butter-comment-assistant/src/butter_comment_assistant/main.py, butter-comment-assistant/src/butter_comment_assistant/web/__init__.py, butter-comment-assistant/tests/conftest.py, butter-comment-assistant/src/butter_comment_assistant/connectors/factory.py, butter-comment-assistant/src/butter_comment_assistant/llm/factory.py, butter-comment-assistant/src/butter_comment_assistant/secrets.py, butter-comment-assistant/src/butter_comment_assistant/logging_setup.py, .planning/phases/01-foundation-and-connectors/01-CONTEXT.md (D-19, D-21, D-23, D-28, D-30)</read_first>
  <action>
Edit `main.py`: in the lifespan, after `run_migrations` and engine creation, call `configure_logging(config.log_level)` (D-16), build `app.state.secret_store` via `resolve_secret_store(config.secret_store, config.data_dir)` (D-15), load `AppSettings` in a `session_scope`, build `app.state.connector` with `build_connector(config.connector, ConnectorOptions(...))` where `token_provider` reads `SECRET_INSTAGRAM_ACCESS_TOKEN` from the secret store at call time (D-23), build `app.state.llm` with `build_llm_client(config.llm, LLMOptions(...))` from the settings (D-28), and set `app.state.auth_status = None`. Do not reference `routes_status` in this task (Task 2 creates that module and adds its `include_router` line to `main.py`). Keep `main()` unchanged apart from calling `configure_logging` instead of `logging.basicConfig`. Ensure nothing here imports `connectors.instagram_graph` or `llm.ollama_client` at module level.

Edit `web/__init__.py` so `shell_context` prefers the live instance: `is_mock = connector.is_mock` and `connector_label = connector.display_name` whenever `request.app.state.connector` is not None (the config fallback remains only for the pre-lifespan case) (D-21).

Extend `tests/conftest.py` (keep the existing `db_url`, `engine`, `session` fixtures) with: `app_env(tmp_path) -> dict` returning `{"BCA_DATA_DIR": str(tmp_path), "BCA_CONNECTOR": "mock", "BCA_LLM": "fake", "BCA_SECRET_STORE": "file"}`; `app(app_env)` returning `create_app(load_config(app_env))`; `client(app)` yielding `fastapi.testclient.TestClient(app)` entered as a context manager; `csrf_client(client)` returning `(client, token)` after one `GET /status` that captures the `bca_csrf` cookie. Do not change the module-level imports used by existing tests.
  </action>
  <verify>
    <automated>cd /home/user/open-saas/butter-comment-assistant && .venv/bin/python -c "
import tempfile, warnings; warnings.simplefilter('ignore')
from fastapi.testclient import TestClient
from butter_comment_assistant.config import load_config
from butter_comment_assistant.main import create_app
d = tempfile.mkdtemp(); app = create_app(load_config({'BCA_DATA_DIR': d, 'BCA_SECRET_STORE': 'file'}))
with TestClient(app) as c:
    assert app.state.connector.is_mock and app.state.llm.kind.value == 'fake' and app.state.secret_store.backend_name.startswith('file:')
    r = c.get('/settings'); assert 'MockConnector (MOCK)' in r.text
print('wiring ok')"</automated>
  </verify>
  <acceptance_criteria>
    - main.py contains `resolve_secret_store(`, `build_connector(`, `build_llm_client(`, `configure_logging(`, `SECRET_INSTAGRAM_ACCESS_TOKEN`
    - main.py does not contain `instagram_graph` or `ollama_client`
    - web/__init__.py reads `connector.is_mock` and `connector.display_name` when a connector is attached
    - tests/conftest.py defines fixtures `app_env`, `app`, `client`, `csrf_client` and still defines `db_url`, `engine`, `session`
    - The verify command prints `wiring ok`
  </acceptance_criteria>
  <done>The app builds its secret store, connector and LLM client from configuration at startup and the shell badge reflects the live connector instance.</done>
</task>

<task type="auto">
  <name>Task 2: Connection status screen with cached auth check and distinct AUTH PROBLEM block</name>
  <files>butter-comment-assistant/src/butter_comment_assistant/web/routes_status.py, butter-comment-assistant/src/butter_comment_assistant/templates/status.html, butter-comment-assistant/src/butter_comment_assistant/main.py, butter-comment-assistant/tests/test_status_screen.py</files>
  <read_first>.planning/phases/01-foundation-and-connectors/01-CONTEXT.md (D-21, D-29), .planning/research/PITFALLS.md (Token expiry silently stalling; Mock connector indistinguishable), butter-comment-assistant/src/butter_comment_assistant/web/__init__.py, butter-comment-assistant/src/butter_comment_assistant/templates/base.html, butter-comment-assistant/src/butter_comment_assistant/templates/settings.html, butter-comment-assistant/src/butter_comment_assistant/connectors/base.py, butter-comment-assistant/src/butter_comment_assistant/connectors/mock.py, butter-comment-assistant/src/butter_comment_assistant/llm/base.py</read_first>
  <action>
Write `web/routes_status.py` per `<interfaces>` (D-29) and add `app.include_router(routes_status.router)` to `create_app` in `main.py` next to the settings router. `get_status_context` computes `auth_status` on first use with `await connector.check_auth()` and caches it on `app.state.auth_status`; `POST /status/check` always re-runs `check_auth()` and stores it; `llm_health` is `await request.app.state.llm.health()` each render (fake is instant; a failing Ollama returns `ok False` rather than raising); `last_sync` reads `SettingsRepo.get_state(session, STATE_LAST_SUCCESSFUL_SYNC_AT)`; `token_present` never includes the token value.

Write `templates/status.html` extending `base.html` with a definition list of the fields and the test ids listed in `<interfaces>`. When `auth.ok` is false render `<div class="alert-auth" data-testid="auth-problem"><strong data-testid="auth-state">AUTH PROBLEM</strong> <span data-testid="auth-error">{{ auth.error }}</span> Collection and sending stay paused until credentials are fixed.</div>`; when ok render `<span data-testid="auth-state">CONNECTED</span>` with account id and username. Always render the documented scopes list, `token-present`, `secret-backend`, `last-sync` ("never" when null), `llm-kind`, `llm-health`, and a "Check connection" form posting to `/status/check` with the hidden `csrf_token` field and `hx-post="/status/check" hx-target="#status-panel" hx-swap="outerHTML"`. No emojis or em dashes.

Write `tests/test_status_screen.py` using the conftest fixtures: (1) `GET /status` returns 200 with `mock-badge`, `connector-kind` containing `mock`, `auth-state` text `CONNECTED`, the two documented scopes as list items, `token-present` `no`, `secret-backend` starting with `file:`, `last-sync` `never`, `llm-kind` `fake`, `llm-health` `ok`; (2) after `SettingsRepo.set_state(session, STATE_LAST_SUCCESSFUL_SYNC_AT, "2026-09-25T12:00:00")` on the app's engine, `GET /status` shows that timestamp; (3) after `app.state.secret_store.set(SECRET_INSTAGRAM_ACCESS_TOKEN, "IGQVJ" + "x"*60)` the page shows `token-present` `yes` and the token string is absent from the body; (4) push `auth_failure` onto `app.state.connector.scenario` for `check_auth`, then `POST /status/check` with the CSRF header returns 200 containing `data-testid="auth-problem"`, `AUTH PROBLEM` and the mock error text, and a following `GET /status` still shows the cached problem; another `POST /status/check` (queue empty) returns to `CONNECTED`; (5) `POST /status/check` without the CSRF token returns 403; (6) with `app_env["BCA_LLM"] = "ollama"` (host default loopback, no daemon), `GET /status` shows `llm-health` `unavailable` and still returns 200.
  </action>
  <verify>
    <automated>cd /home/user/open-saas/butter-comment-assistant && .venv/bin/pytest -q tests/test_status_screen.py</automated>
  </verify>
  <acceptance_criteria>
    - routes_status.py contains `@router.get("/status")`, `@router.post("/status/check")`, `check_auth()`, `STATE_LAST_SUCCESSFUL_SYNC_AT`, `SECRET_INSTAGRAM_ACCESS_TOKEN`, `health()`
    - main.py contains `routes_status.router`
    - templates/status.html contains `data-testid="auth-state"`, `AUTH PROBLEM`, `data-testid="auth-problem"`, `data-testid="scopes"`, `data-testid="last-sync"`, `hx-post="/status/check"`, `name="csrf_token"`
    - tests/test_status_screen.py contains `AUTH PROBLEM`, `token-present`, `unavailable`, and a 403 assertion for the untokened POST
    - `.venv/bin/pytest -q tests/test_status_screen.py` exits 0
  </acceptance_criteria>
  <done>The status screen shows connector type, auth state, scopes, token presence, secret backend, last sync and LLM health, and renders auth failures as a distinct AUTH PROBLEM block.</done>
</task>

<task type="auto">
  <name>Task 3: README with documented commands and the end-to-end smoke test</name>
  <files>butter-comment-assistant/README.md, butter-comment-assistant/tests/test_smoke_foundation.py</files>
  <read_first>.planning/phases/01-foundation-and-connectors/01-CONTEXT.md (D-03, D-12, D-13, D-21), .planning/phases/01-foundation-and-connectors/01-SKELETON.md, butter-comment-assistant/src/butter_comment_assistant/config.py, butter-comment-assistant/src/butter_comment_assistant/main.py, butter-comment-assistant/tests/conftest.py, .planning/BRIEF.md (Standing rules)</read_first>
  <action>
Write `butter-comment-assistant/README.md` in Butter voice (no emojis, no em dashes) with sections: "What this is" (two sentences), "Install" (exactly one command block: `cd butter-comment-assistant && uv venv --python 3.12 .venv && uv pip install --python .venv/bin/python -e ".[dev]"`, plus a one-line pip fallback), "Run" (exactly one command: `.venv/bin/butter-comment-assistant`, then "open http://127.0.0.1:8765"), "Test" (`.venv/bin/pytest`, and `.venv/bin/ruff check src tests`), "Configuration" (a table of the nine `BCA_*` variables with defaults and allowed values from config.py, and a note that app settings are edited at /settings), "Secrets" (keyring or `data/secrets.json` 0600, the two secret names, how the WARNING fallback looks), "Security defaults" (127.0.0.1 only, override variable, CSRF on state-changing requests), "MOCK mode" (default connector is MockConnector, badge on every screen, switching needs `BCA_CONNECTOR=instagram` and a stored token), "Needs a human" (three bullets: Meta app and scopes, real read, one test send; hardware and model selection for Ollama; these are documented fully in the Phase 5 runbook). Do not describe features that do not exist yet (no inbox, no sending).

Write `tests/test_smoke_foundation.py` using the conftest fixtures: (1) walk every GET route in `app.routes` whose path has no path parameters and is not `/static` or `/openapi.json`/`/docs`/`/redoc` (use `follow_redirects=True`), assert each returns 200 and contains `data-testid="mock-badge"` and `data-testid="automation-badge"` (D-21); (2) full restart persistence: change `daily_send_limit` to 11 via `POST /settings` with the CSRF header, close the client, build a second `create_app(load_config(app_env))` and confirm `/settings` shows `value="11"` and `/status` still works; (3) migrations ran: `sqlite_master` on `app_env["BCA_DATA_DIR"]/butter.db` lists the ten tables; (4) README contains the strings `uv venv --python 3.12 .venv`, `.venv/bin/butter-comment-assistant`, `.venv/bin/pytest`, `BCA_ALLOW_NON_LOOPBACK`, `BCA_CONNECTOR` and contains no U+2014 or emoji code points; (5) `pyproject.toml` console script resolves: `importlib.metadata.entry_points(group="console_scripts")` includes `butter-comment-assistant` pointing at `butter_comment_assistant.main:main`; (6) a secrets file written by the app's store in `app_env["BCA_DATA_DIR"]` has mode 0600.

Finally run the complete suite and lint from `butter-comment-assistant/` and record the total test count and runtime in the SUMMARY (must be under 60 seconds, D-30).
  </action>
  <verify>
    <automated>cd /home/user/open-saas/butter-comment-assistant && .venv/bin/pytest -q && .venv/bin/ruff check src tests && grep -q "uv venv --python 3.12 .venv" README.md && grep -q ".venv/bin/butter-comment-assistant" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Install`, `## Run`, `## Test`, `## Configuration`, `BCA_DATA_DIR`, `BCA_SECRET_STORE`, `MOCK` and no U+2014
    - tests/test_smoke_foundation.py contains `mock-badge`, `automation-badge`, `sqlite_master`, `console_scripts`, `0o600`
    - `.venv/bin/pytest -q` exits 0 in under 60 seconds with no network; `.venv/bin/ruff check src tests` exits 0
  </acceptance_criteria>
  <done>The documented install, run and test commands are in the README, the badge shows on every page, settings and schema survive restart, and the full offline suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /status/check | State-changing action that triggers an outbound auth call |
| Status page -> secret store | Page must reveal presence, never value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-27 | Information disclosure | Token value on the status page | mitigate | Only `token_present` boolean is rendered; test asserts token string absent |
| T-01-28 | Spoofing | Cross-site POST /status/check causing auth calls | mitigate | CSRFMiddleware applies; test asserts 403 without token |
| T-01-29 | Repudiation | Auth failure shown as no data | mitigate | AUTH PROBLEM block with distinct test id and red alert style |
| T-01-30 | Repudiation | Real connector shown as mock or vice versa | mitigate | Badge and label come from the live connector instance, not config strings |
| T-01-31 | Denial of service | Status page crashes when LLM daemon absent | mitigate | health() returns ok False instead of raising; test 6 covers it |
| T-01-SC | Tampering | No new packages | accept | none added |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/pytest -q` passes offline in under 60 seconds
- [ ] `.venv/bin/ruff check src tests` passes
- [ ] `BCA_DATA_DIR=/tmp/bca-smoke .venv/bin/butter-comment-assistant` starts and `curl -s http://127.0.0.1:8765/status | grep -c "MOCK CONNECTOR"` prints 1 (then stop the server)
- [ ] README commands match pyproject and config.py
</verification>

<success_criteria>
- App runs end to end on MockConnector and FakeLLMClient from configuration
- Status screen shows connector type, auth state, documented scopes, last sync, token presence, secret backend and LLM health
- MOCK badge on every screen; auth failure distinct
- README documents one install command, one run command and the test command; full suite passes offline
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-connectors/01-07-SUMMARY.md`
</output>
